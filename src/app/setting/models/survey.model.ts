export interface Survey {
  surveyId: string;
  surveyName: string;
  surveyDescription: string;
  surveyType: string;
  createdBy: string;
  updatedBy: string;
  createdAt: string;
  updatedAt: string;
  isDelete: number;
}
